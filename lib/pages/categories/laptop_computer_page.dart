import 'package:flutter/material.dart';

class LaptopComputerPage extends StatelessWidget {
  const LaptopComputerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Laptop & Computer')),
      body: const Center(child: Text('Page for Laptop & Computer')),
    );
  }
}
