import 'package:flutter/material.dart';

class TelevisionAudioPage extends StatelessWidget {
  const TelevisionAudioPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Television & Audio')),
      body: const Center(child: Text('Page for Television & Audio')),
    );
  }
}
